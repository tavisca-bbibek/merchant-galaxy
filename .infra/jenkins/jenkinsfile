pipeline {
    agent {label 'host-pc_lindows_java10'}
        stages {
        stage('Build') {
            steps {
                bat script: '''
                    echo "Building the project."
                    gradlew.bat build
                ''',
            label: "Building Project"
            }
        }
    }
}