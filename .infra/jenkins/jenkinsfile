pipeline {
    agent {label "WindowsPC"}
        stages {
            stage("Build") {
                steps {
                    bat label:'Building',
                    script: '''
                        echo "Building the project."
                        gradlew.bat build
                    '''
                }
            }
    }
}