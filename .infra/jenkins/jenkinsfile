pipeline {
    agent {label "WindowsPC"}
        stages {
            stage("Build") {
                steps {
                    bat script: '''
                        echo "Building the project."
                        gradlew.bat build
                    ''',
                label: "Building Project"
                }
            }
    }
}